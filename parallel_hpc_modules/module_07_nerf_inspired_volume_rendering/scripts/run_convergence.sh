#!/usr/bin/env bash
set -euo pipefail

# TODO: invoke pde_volume_render for N = 8, 16, ..., 512 and append
# sample count, numerical value, analytical value, and relative error to CSV.
echo "Implement the convergence sweep after the solver CLI is available."
